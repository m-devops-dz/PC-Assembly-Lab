/* ============================================================
   CONFIG: photo textures. When self-hosting, put image paths here
   (e.g. "img/5600g-lid.jpg"). Empty = generated textures.
   cpuTop: CPU lid seen from above
   ram:    one side of the RAM heatsink, cropped to the heatsink
   board:  whole motherboard seen from above, cropped to the PCB
   m2:     M.2 SSD label side;  sata: 2.5" SSD top
   gpu:    graphics card fan side;  psu: power supply label side
   Users can also load all of them at once as a .zip theme (see photos.js).
   ============================================================ */
const PHOTO_URLS = { cpuTop: "", ram: "", board: "", m2: "", sata: "", gpu: "", psu: "" };
